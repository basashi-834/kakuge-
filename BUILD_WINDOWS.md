# Windows でのビルド手順（はじめての方向け）

このゲームを、自分のパソコンで `.exe`（実行ファイル）に組み立てる手順です。
**上から順番に、1 つ終わったら次へ**進んでください。

途中でエラーが出たら、**その画面の文字をそのままコピーして教えてください。**
一緒に見ます。エラーは失敗ではなく、次に何をすればいいかを教えてくれる案内です。

所要時間の目安は、**初回だけ 30〜60 分**（うち待ち時間が大半）。
2 回目以降は数分で終わります。

---

## そもそも何をするのか

C++ で書かれたプログラムは、そのままでは動きません。
「人が読む文字」から「パソコンが実行できる形」に翻訳する必要があります。
この翻訳を **ビルド（build）** と言い、そのために次の 3 つを用意します。

| 用意するもの | 役割 |
|---|---|
| **Visual Studio** | 翻訳する道具（C++ コンパイラ）本体 |
| **vcpkg** | 必要な部品（ライブラリ）を自動で取ってくる道具 |
| **SDL2** | 画面を出す・キーを読む・絵を描く部品。このゲームが使う唯一のライブラリ |

---

## 手順 1: Visual Studio をインストールする

すでに入っている方は、この手順を飛ばして **手順 2** へ進んでください。

1. https://visualstudio.microsoft.com/ja/ を開きます。
2. **Visual Studio Community**（無料版）をダウンロードして実行します。
3. インストーラーが起動すると「**ワークロード**」を選ぶ画面が出ます。
   ここで **「C++ によるデスクトップ開発」に必ずチェックを入れて**ください。

> **ここが一番大事な所です。**
> このチェックを入れ忘れると、C++ のコンパイラと CMake が入らず、
> 後の手順で「cl が見つかりません」「cmake は認識されていません」と
> 言われて先に進めなくなります。
> もし入れ忘れたまま進んでしまっても、あとから Visual Studio Installer を
> 起動して「変更」からチェックを追加できます。

4. 「インストール」を押して待ちます（10〜30 分ほど。容量も数 GB 使います）。

---

## 手順 2: Git が使えるか確認する

次の手順で使う vcpkg というツールは、Git を使って取ってきます。

1. **Windows キー**を押して `PowerShell` と入力し、Enter で開きます。
   （黒または青い、文字を打つ画面が出ます）
2. 次のように打って Enter:

   ```
   git --version
   ```

3. `git version 2.xx.x` のようにバージョンが出れば **OK**。手順 3 へ進んでください。
4. 「**用語 'git' は、コマンドレット... として認識されません**」と出た場合は、
   Git が入っていません。https://git-scm.com/download/win からダウンロードして
   インストールしてください。**基本は「次へ」を押し続けるだけ**で大丈夫です。
   インストール後、**PowerShell をいったん閉じて開き直してから**もう一度
   `git --version` を試してください（新しく開かないと反映されません）。

---

## 手順 3: 作業フォルダに移動する

同じ PowerShell の画面で、次を打って Enter:

```
cd C:\
```

これで C ドライブの一番上に移動します。以降の手順はこの場所を前提にします。

> `cd` は「change directory（フォルダを移動する）」の略です。
> 今どこにいるかは、入力欄の左に出ているパスで分かります。

---

## 手順 4: vcpkg をダウンロードする

続けて次を打って Enter:

```
git clone https://github.com/microsoft/vcpkg
```

数十秒〜数分で `C:\vcpkg` というフォルダができます。
文字がたくさん流れますが、**赤字のエラーが出ていなければ成功**です。

---

## 手順 5: vcpkg を使える状態にする

次の 2 つを、**1 行ずつ**、前の行が終わってから打ってください。

```
cd vcpkg
```

```
.\bootstrap-vcpkg.bat
```

2 つ目は初回だけ 1〜2 分かかります。
最後に赤字のエラーが出ていなければ成功です。

> 先頭の `.\` は「今いるフォルダの中にあるこのファイルを実行する」という意味です。
> PowerShell では、これを省くと「見つかりません」と言われます。

---

## 手順 6: SDL2 をインストールする

続けて次を打って Enter:

```
.\vcpkg install sdl2:x64-windows
```

SDL2 をダウンロードして、あなたのパソコン用にビルドします。
**5〜15 分ほどかかります。**途中で止まって見えても進んでいることが多いので、
気長に待ってください。

最後に次のような行が出れば成功です:

```
sdl2:x64-windows ... : SUCCEEDED
```

> `x64-windows` は「64 ビットの Windows 用」という意味です。
> 今のパソコンはほぼすべて 64 ビットなので、これで合っています。

---

## 手順 7: このゲームを設定してビルドする

1. **ゲームのフォルダに移動します。** 解凍した場所に合わせて打ってください。
   例えばダウンロードフォルダに `kakuge` として解凍した場合:

   ```
   cd C:\Users\自分のユーザー名\Downloads\kakuge
   ```

   > 移動できたか不安なときは `dir` と打って Enter。
   > 一覧の中に **`CMakeLists.txt`** があれば、正しい場所にいます。
   > 無ければ、まだ 1 つ上か下の階層にいます。
   >
   > **楽な方法:** エクスプローラーでそのフォルダを開き、
   > 上のアドレス欄をクリックしてパスをコピー → PowerShell に
   > `cd ` と打ってから右クリックで貼り付け、でも移動できます。

2. **設定**（Visual Studio 用のプロジェクトを作る作業）:

   ```
   cmake -B build -S . -DCMAKE_TOOLCHAIN_FILE=C:\vcpkg\scripts\buildsystems\vcpkg.cmake
   ```

   最後に **`-- Build files have been written to: .../build`** と出れば成功です。

3. **ビルド**（実際に翻訳する作業）:

   ```
   cmake --build build --config Release
   ```

   数分かかります。`error C....:` のような**赤い error という行が出なければ成功**です。
   （`warning` は「気になる点のお知らせ」なので、出ていても問題ありません）

---

## 手順 8: できあがった exe を実行する

ゲームのフォルダの中の

```
build\bin\Kakuge.exe
```

これをエクスプローラーでダブルクリックすれば起動します。

**`SDL2.dll` も `data` フォルダも、ビルドのときに自動で隣にコピーされています。**
追加の作業は要りません。

> `.exe` を別の場所に移したいときは、**`build\bin` フォルダごと**移してください。
> `.exe` だけを移すと、隣にあった `SDL2.dll` と `data` を見失って起動できません。

---

## 遊びかた

| キー | 操作 |
|---|---|
| `A` / `D` | 後退 / 前進 |
| `S` | しゃがみ |
| `Space` | ジャンプ |
| `S` ＋ 後ろ | しゃがみガード |
| `U` / `I` / `O` | 弱 / 中 / 強パンチ |
| `J` / `K` / `L` | 弱 / 中 / 強キック |
| `U` ＋ `J` 同時 | 投げ |
| `D` を素早く 2 回 | 前ダッシュ |
| `Esc` | ポーズ |
| `F1` | 当たり判定の表示（トレーニングモード中のみ） |

必殺技のコマンド（数字はテンキーの並びで方向を表します）:

| コマンド | 技 |
|---|---|
| 236 ＋ パンチ | 飛び道具 |
| 623 ＋ パンチ | 対空技 |
| 214 ＋ キック | 回転キック |
| 236236 ＋ パンチ | 超必殺技（ゲージが必要） |

```
7 8 9      7=左上  8=上    9=右上
4 5 6      4=後ろ  5=中立  6=前
1 2 3      1=左下  2=下    3=右下
```

なので **236 = 下 → 斜め下前 → 前** です（いわゆる「波動拳コマンド」）。

---

## エラーが出たときの対処

### `cmake : 用語 'cmake' は認識されません`

Visual Studio の「C++ によるデスクトップ開発」が入っていません。
手順 1 をやり直してください（Visual Studio Installer →「変更」からでも追加できます）。

追加したあとは、**PowerShell を閉じて開き直して**ください。

### `Could NOT find SDL2 (missing: SDL2_DIR)`

原因はほぼ次のどれかです。

1. 手順 6（`.\vcpkg install sdl2:x64-windows`）がまだか、失敗している
   → もう一度実行して、最後に `SUCCEEDED` と出るか確認してください
2. 手順 7-2 の `-DCMAKE_TOOLCHAIN_FILE=...` を打ち忘れている、
   または vcpkg を `C:\vcpkg` 以外の場所に入れた
   → 実際に置いた場所に合わせてパスを書き換えてください
3. `build` フォルダに古い設定が残っている
   → **`build` フォルダをまるごと削除**してから手順 7-2 をやり直してください

### `SDL2 x.x.x が見つかりましたが ... 2.0.18 以降が必要です`

SDL2 が古いです。次の 2 つを順に実行してください。

```
cd C:\vcpkg
git pull
.\bootstrap-vcpkg.bat
.\vcpkg upgrade sdl2:x64-windows --no-dry-run
```

### `SDL2.dll が見つからないため、コードの実行を続行できません`

`.exe` だけを別の場所にコピーした場合に起こります。
`build\bin` フォルダごと移動するか、`build\bin` の中の `.exe` を実行してください。

### Windows に「PC が保護されました」と表示されて起動できない

自分でビルドした署名の無いプログラムに対して、Windows が出す警告です。
**「詳細情報」→「実行」** で起動できます。

Windows 11 の **スマート アプリ コントロール**が「オン」になっていると、
この選択肢すら出ずに問答無用でブロックされることがあります。その場合は
「Windows セキュリティ」→「アプリとブラウザーの制御」→
「スマート アプリ コントロールの設定」から確認してください
（一度オフにすると、Windows の再インストールまで再度オンにできない仕様なので、
慎重に判断してください）。

### 画面が真っ黒のまま / ウィンドウは出るが何も表示されない

古い PC やリモートデスクトップ経由だと、GPU 描画が使えないことがあります。
その場合はソフトウェア描画に切り替えて起動してみてください。
PowerShell で、ゲームのフォルダから:

```
$env:SDL_RENDER_DRIVER="software"
.\build\bin\Kakuge.exe
```

これで映れば、GPU ドライバの更新で直る可能性が高いです。

---

## 2 回目以降のビルド

ソースコードを書き換えたあとは、**手順 7-3 だけ**でかまいません。

```
cmake --build build --config Release
```

設定（手順 7-2）が必要なのは、**新しいファイルを追加したとき**や
`CMakeLists.txt` を書き換えたときだけです。

---

## Visual Studio の画面で開きたい場合

コマンドを打つ代わりに、Visual Studio の中で編集・実行することもできます。

1. 手順 7-2（設定）まで済ませる
2. `build\Kakuge.sln` をダブルクリックして Visual Studio で開く
3. 画面上部の構成を **Release** にする
4. ソリューションエクスプローラーで **Kakuge** を右クリック →
   「**スタートアップ プロジェクトに設定**」
5. `F5` キーで実行

---

## 動作確認用のテスト

戦闘のルール（当たり判定・フレーム処理・ガードの成立条件など）だけを
画面なしで検証するプログラムも一緒にビルドされています。

```
.\build\bin\EngineTests.exe
```

最後に `RESULT: 68 passed, 0 failed` と出れば、エンジンは正常です。
ゲームの調整をしたあと、壊していないか確かめるのに使ってください。
