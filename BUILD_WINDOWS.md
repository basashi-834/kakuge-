# Windows でのビルド手順（はじめての方向け）

このゲームを、自分のパソコンで `.exe`（実行ファイル）に組み立てる手順です。
**上から順番に、1 つ終わったら次へ**進んでください。

途中でエラーが出たら、**その画面の文字をそのままコピーして教えてください。**
一緒に見ます。エラーは失敗ではなく、次に何をすればいいかを教えてくれる案内です。

所要時間の目安は、**初回だけ 20〜60 分**（うち待ち時間が大半）。
2 回目以降は数分で終わります。

---

## そもそも何をするのか

C++ で書かれたプログラムは、そのままでは動きません。
「人が読む文字」から「パソコンが実行できる形」に翻訳する必要があります。
この翻訳を **ビルド（build）** と言い、そのために次の 2 つを用意します。

| 用意するもの | 役割 |
|---|---|
| **Visual Studio** | 翻訳する道具（C++ コンパイラ）本体 |
| **SDL2** | 画面を出す・キーを読む・絵を描く部品。このゲームが使う唯一のライブラリ |

---

## 道は 2 つあります（好きなほうを選んでください）

違いは **SDL2 の入手方法だけ**です。どちらを選んでも、
できあがるゲームはまったく同じものになります。

| | **ルート A: 直接ダウンロード** | **ルート B: vcpkg を使う** |
|---|---|---|
| Git | **不要** | 必要 |
| 手順の数 | 5 つ | 9 つ |
| 待ち時間 | 1 分ほど | 10〜20 分ほど |
| 向いている人 | **はじめての方・すぐ動かしたい方** | 今後ライブラリを増やしたい方 |

**迷ったらルート A を選んでください。** 速くて手順も少なく、
Git のインストールも要りません。

- ルート A → 手順 1 のあと **手順 A-1** へ（手順 5 つ）
- ルート B → 手順 1 のあと **手順 B-1** へ

---

## 手順 1: Visual Studio をインストールする

すでに入っている方は、この手順を飛ばして **ルート A**（または **ルート B**）へ進んでください。

1. https://visualstudio.microsoft.com/ja/ を開きます。
2. **Visual Studio Community**（無料版）をダウンロードして実行します。
3. インストーラーが起動すると「**ワークロード**」を選ぶ画面が出ます。
   ここで **「C++ によるデスクトップ開発」に必ずチェックを入れて**ください。

> **ここが一番大事な所です。**
> このチェックを入れ忘れると、C++ のコンパイラと CMake が入らず、
> 後の手順で「cl が見つかりません」「cmake は認識されていません」と
> 言われて先に進めなくなります。
> もし入れ忘れたまま進んでしまっても、あとから Visual Studio Installer を
> 起動して「変更」からチェックを追加できます。

4. 「インストール」を押して待ちます（10〜30 分ほど。容量も数 GB 使います）。

---

## ルート A: SDL2 を直接ダウンロードする（Git 不要）

> ルート B（vcpkg）を選んだ方は、ここを飛ばして **手順 B-1** へ進んでください。

### 手順 A-1: SDL2 をダウンロードする

**次のリンクをクリックしてください。それだけです。**

https://github.com/libsdl-org/SDL/releases/download/release-2.32.10/SDL2-devel-2.32.10-VC.zip

`SDL2-devel-2.32.10-VC.zip`（7MB ほど）がダウンロードされます。1 分もかかりません。

> **⚠️ 「SDL3 しか見つからない」ときは、これが原因です**
>
> SDL の公式ページ（`/releases/latest`）を開くと、いま表示されるのは
> **SDL3** です。SDL2 と SDL3 は別物の新旧 2 系統で、
> 「最新」の場所には新しい SDL3 のほうが出てくるからです。
>
> **SDL2 は消えたわけではなく、今も同じ場所で配布が続いています。**
> ただし「最新」ページには出てこないので、上の直リンクを使うのが確実です。
>
> 自分で探したい場合は、`/releases/latest` ではなく
> [リリース一覧](https://github.com/libsdl-org/SDL/releases) を開き、
> ページ左の一覧から **`SDL2 2.32.x`** という名前のものを選んでください
>（`SDL3 3.x.x` のほうではありません）。
>
> ちなみに **このゲームを SDL3 で作り直す必要はありません。**
> SDL2 は今も更新が続いている現役のバージョンです。

> **ファイル名の見分け方**（別のバージョンを選ぶ場合）
> - `SDL2-` で始まる … **SDL3 ではなく SDL2**。必須
> - `devel` … 開発用（プログラムを作る人向け）。必須
>   （付いていないものは「遊ぶ人向け」で、中身が足りません）
> - `VC` … Visual C++ 用。必須（`mingw` 版は Visual Studio では使えません）

### 手順 A-2: 解凍して、ゲームのフォルダの隣に置く

1. ダウンロードした zip を右クリック →「**すべて展開**」で解凍します。
2. `SDL2-2.32.10` という名前のフォルダができます。
3. **そのフォルダを、`CMakeLists.txt` があるフォルダの中へ移動します。**

置いたあとは、こういう形になっていれば正解です。

```
kakuge\
  CMakeLists.txt          ← これと
  BUILD_WINDOWS.md
  src\
  data\
  SDL2-2.32.10\           ← これが同じ階層に並ぶ
      include\
      lib\
      cmake\             ← このフォルダがあることが目印
```

> **なぜこの置き方でいいのか**
> `CMakeLists.txt` に「隣に SDL2 フォルダがあれば自動で使う」という
> 仕組みを入れてあります。だから場所さえ合っていれば、
> 面倒なオプション指定は一切いりません。
>
> フォルダ名は `SDL2-` で始まっていれば、後ろの数字は何でも構いません。
> 単に `SDL2` という名前にしても大丈夫です。

### 手順 A-3: 「開発者コマンドプロンプト」を開く

**⚠️ ここが一番つまずきやすい所です。**

**普通のコマンドプロンプトや PowerShell を使ってはいけません。**
Visual Studio に付属する `cmake` は、専用の画面からでないと使えないためです。

1. **Windows キー**を押して、次のように入力します:

   ```
   x64 Native Tools
   ```

2. 検索結果に出てくる
   **「x64 Native Tools Command Prompt for VS 2022」** をクリックして開きます。

   > 「Developer Command Prompt for VS 2022」でも構いません。
   > VS 2019 をお使いなら末尾が 2019 になります。

3. 開いた画面に、次のような行が出ていれば正解です:

   ```
   **********************************************************************
   ** Visual Studio 2022 Developer Command Prompt v17.x.x
   ```

4. 動作確認のため、次を打って Enter:

   ```
   cmake --version
   ```

   `cmake version 3.xx.x` と出れば準備完了です。

> **なぜ普通の画面ではダメなのか**
>
> `cmake` や C++ コンパイラ（`cl`）は Visual Studio のフォルダの奥深くに
> 入っています。普通のコマンドプロンプトはそこを探しに行かないので、
> 「認識されていません」と言われます。
>
> 「開発者コマンドプロンプト」は、**それらの場所を最初に教えてから**
> 開いてくれる専用の画面です。だからそのまま `cmake` と打てます。

### 手順 A-4: 設定する

開いた開発者コマンドプロンプトで、ゲームのフォルダに移動します
（実際に解凍した場所に合わせてください）:

```
cd C:\Users\自分のユーザー名\Downloads\kakuge
```

> 移動できたか不安なときは `dir` と打って Enter。
> 一覧に **`CMakeLists.txt`** があれば、正しい場所にいます。
>
> **楽な方法:** エクスプローラーでそのフォルダを開き、上のアドレス欄を
> クリックしてパスをコピー → 画面に `cd ` と打ってから
> 右クリックで貼り付け、でも移動できます。
>
> **注意:** GitHub から ZIP をダウンロードした場合、解凍すると
> `kakuge--claude-...\kakuge--claude-...\` のように**同じ名前のフォルダが
> 二重**になっていることがあります。`dir` で `CMakeLists.txt` が
> 見えなければ、`cd` でもう 1 つ内側に入ってください。

移動できたら、次の 1 行を打って Enter:

```
cmake -B build -S .
```

途中に次の行が出れば、SDL2 が見つかっています。

```
-- SDL2 をこの場所で見つけました: .../SDL2-2.32.10
```

最後に **`-- Build files have been written to: .../build`** と出れば成功です。

### 手順 A-5: ビルドする

```
cmake --build build --config Release
```

数分かかります。`error C....:` のような**赤い error という行が出なければ成功**です。
（`warning` は「気になる点のお知らせ」なので、出ていても問題ありません）

**これで完了です。** → **手順 8** へ進んでください。

---

## ルート B: vcpkg を使う（Git が必要）

> ルート A で成功した方は、ここは読まなくて大丈夫です。→ **手順 8** へ

vcpkg は、必要な部品を自動で取ってきてビルドしてくれる道具です。
今後 SDL2 以外のライブラリも足していきたい場合は、こちらが便利です。

### 手順 B-1: Git が使えるか確認する

1. **Windows キー**を押して `x64 Native Tools` と入力し、検索結果の
   **「x64 Native Tools Command Prompt for VS 2022」** を開きます。
   （普通のコマンドプロンプトや PowerShell ではなく、これを使ってください。
   　理由は手順 A-3 の説明と同じです）
2. 次のように打って Enter:

   ```
   git --version
   ```

3. `git version 2.xx.x` のようにバージョンが出れば **OK**。次へ進んでください。
4. 「**用語 'git' は、コマンドレット... として認識されません**」と出た場合は、
   Git が入っていません。https://git-scm.com/download/win からダウンロードして
   インストールしてください。**基本は「次へ」を押し続けるだけ**で大丈夫です。
   インストール後、**画面をいったん閉じて開き直してから**もう一度
   `git --version` を試してください（新しく開かないと反映されません）。

### 手順 B-2: 作業フォルダに移動する

```
cd C:\
```

これで C ドライブの一番上に移動します。以降の手順はこの場所を前提にします。

> `cd` は「change directory（フォルダを移動する）」の略です。

### 手順 B-3: vcpkg をダウンロードする

```
git clone https://github.com/microsoft/vcpkg
```

数十秒〜数分で `C:\vcpkg` というフォルダができます。
文字がたくさん流れますが、**赤字のエラーが出ていなければ成功**です。

### 手順 B-4: vcpkg を使える状態にする

次の 2 つを、**1 行ずつ**、前の行が終わってから打ってください。

```
cd vcpkg
```

```
.\bootstrap-vcpkg.bat
```

2 つ目は初回だけ 1〜2 分かかります。

> 先頭の `.\` は「今いるフォルダの中にあるこのファイルを実行する」という意味です。
> これを省くと「見つかりません」と言われることがあります。

### 手順 B-5: SDL2 をインストールする

```
.\vcpkg install sdl2:x64-windows
```

SDL2 をダウンロードして、あなたのパソコン用にビルドします。
**5〜15 分ほどかかります。**途中で止まって見えても進んでいることが多いので、
気長に待ってください。

最後に次のような行が出れば成功です:

```
sdl2:x64-windows ... : SUCCEEDED
```

> `x64-windows` は「64 ビットの Windows 用」という意味です。
> 今のパソコンはほぼすべて 64 ビットなので、これで合っています。

---

## 手順 B-6: このゲームを設定してビルドする

1. **ゲームのフォルダに移動します。** 解凍した場所に合わせて打ってください。
   例えばダウンロードフォルダに `kakuge` として解凍した場合:

   ```
   cd C:\Users\自分のユーザー名\Downloads\kakuge
   ```

   > 移動できたか不安なときは `dir` と打って Enter。
   > 一覧の中に **`CMakeLists.txt`** があれば、正しい場所にいます。
   > 無ければ、まだ 1 つ上か下の階層にいます。
   >
   > **楽な方法:** エクスプローラーでそのフォルダを開き、
   > 上のアドレス欄をクリックしてパスをコピー → 画面に
   > `cd ` と打ってから右クリックで貼り付け、でも移動できます。

2. **設定**（Visual Studio 用のプロジェクトを作る作業）:

   ```
   cmake -B build -S . -DCMAKE_TOOLCHAIN_FILE=C:\vcpkg\scripts\buildsystems\vcpkg.cmake
   ```

   最後に **`-- Build files have been written to: .../build`** と出れば成功です。

3. **ビルド**（実際に翻訳する作業）:

   ```
   cmake --build build --config Release
   ```

   数分かかります。`error C....:` のような**赤い error という行が出なければ成功**です。
   （`warning` は「気になる点のお知らせ」なので、出ていても問題ありません）

---

## 手順 8: できあがった exe を実行する

ゲームのフォルダの中の

```
build\bin\Kakuge.exe
```

これをエクスプローラーでダブルクリックすれば起動します。

**`SDL2.dll` も `data` フォルダも、ビルドのときに自動で隣にコピーされています。**
追加の作業は要りません。

> `.exe` を別の場所に移したいときは、**`build\bin` フォルダごと**移してください。
> `.exe` だけを移すと、隣にあった `SDL2.dll` と `data` を見失って起動できません。

---

## 遊びかた

| キー | 操作 |
|---|---|
| `A` / `D` | 後退 / 前進 |
| `S` | しゃがみ |
| `Space` | ジャンプ |
| `S` ＋ 後ろ | しゃがみガード |
| `U` / `I` / `O` | 弱 / 中 / 強パンチ |
| `J` / `K` / `L` | 弱 / 中 / 強キック |
| `U` ＋ `J` 同時 | 投げ |
| `D` を素早く 2 回 | 前ダッシュ |
| `Esc` | ポーズ |
| `F1` | 当たり判定の表示（トレーニングモード中のみ） |

必殺技のコマンド（数字はテンキーの並びで方向を表します）:

| コマンド | 技 |
|---|---|
| 236 ＋ パンチ | 飛び道具 |
| 623 ＋ パンチ | 対空技 |
| 214 ＋ キック | 回転キック |
| 236236 ＋ パンチ | 超必殺技（ゲージが必要） |

```
7 8 9      7=左上  8=上    9=右上
4 5 6      4=後ろ  5=中立  6=前
1 2 3      1=左下  2=下    3=右下
```

なので **236 = 下 → 斜め下前 → 前** です（いわゆる「波動拳コマンド」）。

---

## エラーが出たときの対処

### `'cmake' は、内部コマンドまたは外部コマンド ... として認識されていません`

（PowerShell では `cmake : 用語 'cmake' は認識されません` と出ます）

**原因の 9 割は「開く画面を間違えている」ことです。** 上から順に確認してください。

**1. 普通のコマンドプロンプト / PowerShell を使っていませんか？**

Visual Studio に付属する `cmake` は、Windows 全体からは見えない場所に
入っています。**「x64 Native Tools Command Prompt for VS 2022」**
を開き直してください（手順 A-3 を参照）。これが最も多い原因です。

**2. その画面が検索しても見つからない場合**

Visual Studio の「**C++ によるデスクトップ開発**」が入っていません。

1. Windows キー → `Visual Studio Installer` を開く
2. インストール済みの Visual Studio の「**変更**」を押す
3. 「**C++ によるデスクトップ開発**」にチェックを入れて「変更」
4. 終わったら PC を再起動し、もう一度「x64 Native Tools」を検索

**3. それでも解決しない / 普通の画面で使いたい場合**

CMake を単独でインストールすれば、どの画面からでも使えるようになります。

1. https://cmake.org/download/ を開く
2. **Windows x64 Installer**（`cmake-3.xx.x-windows-x86_64.msi`）を入れる
3. インストール中の
   「**Add CMake to the system PATH for all users**」に**必ずチェック**
4. **画面を閉じて開き直す**（開き直さないと反映されません）

ただしこの方法でも、C++ コンパイラ本体は Visual Studio のものが必要です。
手順 1 は必ず済ませてください。

### `Could NOT find SDL2 (missing: SDL2_DIR)`

**ルート A（直接ダウンロード）の場合:**

1. SDL2 フォルダの**置き場所**が違う
   → `CMakeLists.txt` と**同じ階層**に置いてください。
   置いたフォルダの中に **`cmake`** というフォルダがあるかも確認を。
2. zip の**種類**が違う
   → `SDL2-devel-2.xx.x-**VC**.zip` が必要です。
   `mingw` 版や、`devel` の付いていないもの（実行用）では中身が足りません。
3. フォルダを**二重に**解凍している
   → `SDL2-2.32.10\SDL2-2.32.10\cmake\...` のようになっていたら、
   内側のフォルダを 1 つ上に出してください。
4. `build` フォルダに古い設定が残っている
   → **`build` フォルダをまるごと削除**してからやり直してください

**ルート B（vcpkg）の場合:**

1. 手順 B-5（`.\vcpkg install sdl2:x64-windows`）がまだか、失敗している
   → もう一度実行して、最後に `SUCCEEDED` と出るか確認してください
2. `-DCMAKE_TOOLCHAIN_FILE=...` を打ち忘れている、
   または vcpkg を `C:\vcpkg` 以外の場所に入れた
   → 実際に置いた場所に合わせてパスを書き換えてください
3. `build` フォルダに古い設定が残っている
   → **`build` フォルダをまるごと削除**してからやり直してください

### `SDL2 x.x.x が見つかりましたが ... 2.0.18 以降が必要です`

SDL2 が古いです。

**ルート A の場合:** 古い SDL2 フォルダを削除し、手順 A-1 の直リンクから
新しい `SDL2-devel-2.32.10-VC.zip` を取り直してください。
そのあと `build` フォルダを削除してからやり直します。

**ルート B の場合:** 次を順に実行してください。

```
cd C:\vcpkg
git pull
.\bootstrap-vcpkg.bat
.\vcpkg upgrade sdl2:x64-windows --no-dry-run
```

### `SDL2.dll が見つからないため、コードの実行を続行できません`

`.exe` だけを別の場所にコピーした場合に起こります。
`build\bin` フォルダごと移動するか、`build\bin` の中の `.exe` を実行してください。

### Windows に「PC が保護されました」と表示されて起動できない

自分でビルドした署名の無いプログラムに対して、Windows が出す警告です。
**「詳細情報」→「実行」** で起動できます。

Windows 11 の **スマート アプリ コントロール**が「オン」になっていると、
この選択肢すら出ずに問答無用でブロックされることがあります。その場合は
「Windows セキュリティ」→「アプリとブラウザーの制御」→
「スマート アプリ コントロールの設定」から確認してください
（一度オフにすると、Windows の再インストールまで再度オンにできない仕様なので、
慎重に判断してください）。

### 画面が真っ黒のまま / ウィンドウは出るが何も表示されない

古い PC やリモートデスクトップ経由だと、GPU 描画が使えないことがあります。
その場合はソフトウェア描画に切り替えて起動してみてください。
コマンドプロンプトで、ゲームのフォルダから:

```
set SDL_RENDER_DRIVER=software
build\bin\Kakuge.exe
```

（PowerShell の場合は `$env:SDL_RENDER_DRIVER="software"` と書きます）

これで映れば、GPU ドライバの更新で直る可能性が高いです。

---

## 2 回目以降のビルド

ソースコードを書き換えたあとは、**ビルドのコマンドだけ**でかまいません。

```
cmake --build build --config Release
```

設定（`cmake -B build -S .`）をやり直す必要があるのは、
**新しいファイルを追加したとき**や `CMakeLists.txt` を書き換えたときだけです。

---

## Visual Studio の画面で開きたい場合

コマンドを打つ代わりに、Visual Studio の中で編集・実行することもできます。

1. 設定（ルート A なら手順 A-3、ルート B なら手順 B-6-2）まで済ませる
2. `build\Kakuge.sln` をダブルクリックして Visual Studio で開く
3. 画面上部の構成を **Release** にする
4. ソリューションエクスプローラーで **Kakuge** を右クリック →
   「**スタートアップ プロジェクトに設定**」
5. `F5` キーで実行

---

## 動作確認用のテスト

戦闘のルール（当たり判定・フレーム処理・ガードの成立条件など）だけを
画面なしで検証するプログラムも一緒にビルドされています。

```
.\build\bin\EngineTests.exe
```

最後に `RESULT: 68 passed, 0 failed` と出れば、エンジンは正常です。
ゲームの調整をしたあと、壊していないか確かめるのに使ってください。
